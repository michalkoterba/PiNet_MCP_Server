# PiNet API Configuration
# The base URL of your PiNet API instance
# Format: http://YOUR_PINET_IP:PORT
# Note: PiNet API typically runs on port 5000 by default
# Examples:
#   - http://192.168.1.100:5000
#   - http://raspberrypi.local:5000
#   - http://10.0.0.50:5000
PINET_API_URL=http://YOUR_PINET_IP:5000

# The API key for authenticating with PiNet API
# Get this from your PiNet API server
PINET_API_KEY=your_api_key_here

# MCP Server Configuration (Optional)
# Port for the MCP server to listen on (default: 8000)
MCP_SERVER_PORT=8000

# Logging Configuration (Optional)
# Set the logging level for the MCP server
# Valid values: DEBUG, INFO, WARNING, ERROR, CRITICAL
# - DEBUG: Most detailed logs, includes all API calls and parameters from LLM
# - INFO: Normal operational logs, shows tool calls and responses (default)
# - WARNING: Only warnings and errors
# - ERROR: Only error messages
# Use DEBUG when troubleshooting LLM parameter formatting issues
LOG_LEVEL=INFO

# Tailscale Configuration (Only needed for docker-compose-tailscale.yml)
# Get your auth key from: https://login.tailscale.com/admin/settings/keys
# TS_AUTHKEY=tskey-auth-xxxxxxxxxxxxxxxxxxxxx